# FretFormula implementation progress

## Baseline
- Date/time: 2026-09-06 (Europe/Moscow); resumed 2026-09-07 and 2026-09-08.
- Existing state: Godot 4.7.2, GL Compatibility; one drawn Control scene, GuitarTuning and MusicTheory, five domain assertions. UI hard-codes A minor pentatonic, six strings and placeholder region windows. Existing global bridge preserved.
- Checks: headless editor imports but reports 6 leaked ObjectDB instances / 1 resource on exit; domain runner and 90-frame main scene smoke pass.

## Current stage
- Stage: stage 8 and the 2026-09-08 visual-reference matching pass are complete as a desktop/Android emulator candidate.
- Goal: keep the reference-matched candidate reproducible while preserving honest physical-device verification boundaries.
- Acceptance status: the main screen now follows the supplied FretFormula reference hierarchy, physical fret spacing, realistic fretboard treatment and settings overlay; both guitar timbres and polyphony remain integrated. Physical-device audio, latency, multitouch and lifecycle remain unverified.

## Completed
- [x] Fingering duplicate correction: the ascending path now rejects overlapping adjacent-string positions at or below the previous MIDI pitch. The 21 × 12 fingering regression asserts strictly increasing MIDI throughout the ascent. A Lydian contains D♯3 and D♯4 at distinct physical positions, which is intentional octave repetition in a multi-octave run, not a duplicate sounding pitch.
- [x] Data-driven scale fingering: `ScaleFingering` derives exact playable `string_index + fret` paths from the rendered model, anchored at the sixth-string tonic with the lower-string reach needed by standard guitar patterns. Playback travels the exact 6→1 path and its reverse; the highlighted marker now identifies that exact physical position. Tests cover every 21-scale/12-tonic combination for unique in-scale positions and exact reverse traversal, plus the corrected A natural-minor reference path including B on string 3/fret 4.
- [x] Box-scale playback and tempo: scale playback now selects the adaptive five-fret box anchored at the nearest sixth-string tonic, plays its displayed positions string 6→1, then returns without repeating the top note. The header has a persisted Tempo field (30–240 BPM, default 60); each scale step is one quarter note at the selected BPM. Settings and scene integration tests pass.
- [x] Scale playback feedback and timbre refinement: the sequence player now marks its exact currently sounding MIDI note(s) on the fretboard with a cyan playback ring without affecting touch-owned voices. The scale-play control uses a shared dark/cyan theme variation and enters an active state during playback. Procedural acoustic synthesis gained layered soundboard resonance; electric synthesis gained a fuller pickup blend and stronger soft-clipped gain. Sequence, timbre and integrated scene tests pass; audible quality still needs human/device listening.
- [x] Scale-library expansion: the central catalog now contains 21 ordered scales/modes in nine UI categories. Each record has interval/degree data plus characteristic offsets, compact teaching text, character and typical-chord metadata; `ScaleDefinition.note_role()` supplies ROOT/CHARACTERISTIC/SECONDARY to the fretboard. The visual palette is now interval-functional: all 12 chromatic degrees have distinct stable colors across every tonic, with tonic retaining the root color. Formula, catalog, application, fretboard-view and integrated scene checks pass. A fresh all-check runner import gate exceeded its 45-second timeout; physical-device audio and multitouch remain unverified.
- [x] Read specification, roadmap, README, project settings, existing scene/domain/UI/tests and applicable instructions.
- [x] Run baseline import, domain and main scene checks.
- [x] Fixed bridge shutdown reference cycle; independently reran import with no resource leak, domain tests and 60-frame smoke.
- [x] Added Pitch, Interval, ScaleDefinition, contextual NoteSpelling and FretPosition; validated arbitrary tunings. Parent tests cover -24..151 MIDI, octave boundaries, 1/6/7 strings and frets 0..24, all tonic transpositions, 440/880 Hz, raw invalid inputs and enharmonic cases.
- [x] Stage 1 final import/domain/60-frame smoke pass cleanly, including canonical Interval labels, sorted formulas and bridge UID normalization.
- [x] Stage 2: 12 canonical scale formulas, two aliases, four tunings, separate loader/validator and atomic user fallback. Import, catalog fixtures, domain, formula matrix and 90-frame smoke pass via tests/run_checks.ps1.
- [x] Stage 3: ScaleExplorer and view-model builder, working tonic/scale/label selectors, one geometry for rendering and hit tests, scrollable 24-fret view, exact position details and independent pointer IDs at the view boundary.
- [x] Stage 3 verification: import; application/catalog/domain/formula/geometry/scene runners; main-scene smoke; GPU-rendered screenshots at 640x360, 1280x720 and 1920x1080 inspected. The 640x360 layout remains functional through horizontal/vertical scrolling.
- [x] Stage 4: versioned five-box pentatonic positions and relative C/A/G/E/D shapes; major/minor/diminished tonic-triad role adaptation, octave instances, overlaps, fingering and honest standard-tuning limits.
- [x] Stage 4 verification: 12 tonics x 5 boxes x major/minor, 12 tonics x 5 CAGED forms for major/minor/Locrian, MIDI invariants, unique coordinate merging, selector/filter scene tests, full runner and visible position/CAGED GPU screenshots.
- [x] Stage 5 prototype: compared AudioStreamPolyphonic, 12 AudioStreamGenerator players and 12 cached-WAV players on installed Godot 4.7.2 using Dummy and Windows WASAPI. The explicit WAV player pool passed independent fade/stop/reuse; Polyphonic stop/reclaim failed in both drivers.
- [x] Stage 5 implementation: deterministic 12-slot opaque-handle pool; cached autonomous plucked synthesis; eight octave anchor WAVs with <=6-semitone pitch shift; 60 ms release; safe summed gain; synchronous all-notes-off.
- [x] Stage 5 verification: allocation/release/stealing/stale-handle tests; 12 live WAV players; exact A4 formula and pitch ratio; focus cleanup; clean import and complete runner. API/runtime behavior is verified, audibility and latency are not.
- [x] Stage 6: backend-independent owner-to-handle coordinator, explicit retrigger policy, empty-zone/re-entry handling, mouse owner adapter and independent ScreenTouch indices connected to the fretboard.
- [x] Stage 6 verification: focused 2/6/12-contact tests plus real scene integration with six synthetic owners, same-cell deduplication, cross-cell retrigger, isolated release and focus cleanup. Complete runner passes after audio-thread teardown wait.
- [x] Stage 7: versioned atomic settings with validated fallback; standard/alternate/custom tunings, 12–36 frets, mirroring, enharmonic preference, volume, display/study modes and RU/EN localization are accessible through the settings panel.
- [x] Stage 7 responsive/accessibility verification: text and shape cues supplement colour, touch targets and labels have focused tests, phone/tablet/desktop policies include DPI scaling and display-safe-area margins, and visible desktop plus Android phone/tablet emulator layouts were inspected.
- [x] Android candidate packaging: official Godot 4.7.2 export templates were installed after SHA-512 verification; Android Build-Tools 36.0.0 is used; the debug APK targets SDK 36/min SDK 24 and contains arm64-v8a plus x86_64 libraries. Project resources exclude docs and tests.
- [x] Android emulator verification: the candidate starts on 2400x1080/420 dpi phone and 2560x1600/320 dpi tablet AVDs in landscape. Phone HOME/resume retained the process and produced no app/script/fatal errors; tablet reached `OnGodotMainLoopStarted` without app/script/fatal errors.
- [x] Candidate documentation: README, development-plan status and third-party notices describe implemented behavior, build steps, licenses and verification gaps.
- [x] Stage 8 harmony: strict versioned JSON catalogs define nine common chords and nine ascending/descending/up-down arpeggios. Definitions validate roots, intervals, ordering, octave offsets and chord-tone membership.
- [x] Stage 8 playback: the UI can play the selected scale, chord or arpeggio through an isolated sequence player; cancellation releases only sequence-owned handles and lifecycle pause stops playback.
- [x] Stage 8 practice: a seedable UI-independent note-finding session accepts `string_index + fret`, tracks attempts/correct/streak, and persists versioned aggregate statistics with atomic replacement and damaged-file fallback.
- [x] Stage 8 instrument profiles: standard 4-string bass and high-G ukulele presets reuse the existing arbitrary-string tuning and fretboard pipeline; unsupported CAGED combinations remain explicitly reported.
- [x] Stage 8 visible integration: the harmony/practice panel was rendered with the desktop GPU and scene tests cover catalog loading, scale/chord playback, cancellation, task answers and persistence boundaries.
- [x] Stage 8 Android integration: the final APK was reinstalled on both AVDs; phone rendering with the wrapped panel and a practice task was inspected after real UI taps, both processes remained live, and logs contained no app/script/fatal error. The emulator shader-cache recompilation warning remains expected and non-fatal.
- [x] Android launch/menu correction: removed the manual DPI `content_scale_factor` that diverged from the project transform, then adopted the standard `canvas_items`/`expand` stretch path so rendering and input share one scaled coordinate space. Expensive procedural anchor generation remains outside startup. Native `PopupMenu` was replaced by an in-scene button list because the Android popup painted but did not complete a touch selection.
- [x] Guitar timbres: added acoustic/electric profiles, a persisted localized selector, 16 reproducibly generated 44.1 kHz/16-bit/2.4 s PCM anchor WAVs and a procedural fallback. Tests verify distinct waveform data, upper-harmonic energy and the longer electric sustain.
- [x] Chord interaction: the existing contact-owned 12-voice engine now uses the selected timbre for every independent voice. Tests hold 6 and allocate 12 simultaneous notes, then release/move one owner without affecting the others.
- [x] Responsive/visual correction: the fretboard has no horizontal overflow minimum, fits all frets into its parent width, and shows all six strings in the 640x360 render. Position and CAGED metadata render as translucent labelled boxes. Header, mode row, settings and controls use a unified modern dark card theme with compact mobile labels.
- [x] Visual-reference matching: rebuilt the main presentation as Header/Fretboard/Footer, added labelled tonic and scale selectors, a true Notes/Degrees/Both segmented control, circular Settings/Help actions, a footer legend and persistent scale-play action. Secondary context, tuning, instrument, fret-count, mirror, spelling, volume, language, harmony and diagnostics live in overlay panels instead of the main surface.
- [x] Physical fretboard rendering: one shared geometry applies a reference-oriented 12-TET spacing curve for both draw and hit-test. The desktop board is now neck-only and uses a classical-guitar-inspired long-to-wide proportion: fret spacing is unchanged horizontally, while the neck and string pitch are 1.5x taller. It retains layered natural wood grain, restrained metallic frets, six weighted strings, enlarged dimensional tonic/scale markers, nut, open-string labels, classical single/double markers and selective fret numbers.
- [x] Fretboard zoom and navigation: mouse-wheel and pinch zoom use one scale factor for the fretboard surface, strings, frets and note markers. The scroll container receives the gesture before child controls; the vertical zoom limit is derived from the space between header and footer, while horizontal content grows inside an automatically scrollable fretboard container.
- [x] Live fretboard footer status: the former legend is replaced with the currently sounding note names; simultaneous notes are reduced to note labels plus a detected chord name when the pitch-class set matches the chord catalog.
- [x] Position tinting and initial framing: note markers inherit stable per-position hues from region metadata, while the initial board fills the available width and centers its neck surface vertically in the header/footer gap.
- [x] Main-surface simplification: note labels are permanently shown as note plus degree, their outline is antialiased, and the scale-play action now occupies the former display-mode slot in the header. Footer zoom controls and percentage are removed.
- [x] Desktop launch stability: prevent duplicate scroll-layout recalculation for an unchanged viewport and enable desktop VSync, avoiding the GUI main-thread saturation observed after interactive launches.

## Next
- [ ] Before mobile Done, verify audible output/latency, independent real multitouch and lifecycle on at least one physical target device.
- [ ] Any further content expansion beyond the explicit stage 8 bullets should be scoped as a new change rather than folded into the verified candidate.

## Decisions
- Follow SPEC_ASTRA.md section 16 order. No Git initialization or added dependencies.
- Preserve GuitarTuning/MusicTheory compatibility while adding domain types.
- Delegated bridge shutdown diagnosis to gpt-5.6-terra / medium, addons/godot_ai_bridge only; reference-cycle fix reviewed and accepted. UID autoload normalization follow-up pending.
- Delegated domain types to gpt-5.6-terra / medium, scripts/domain only; independent domain tests pass; canonical degree/ordering follow-up pending.
- Stage 1 follow-ups accepted after clean integrated CLI checks. Stage 2 loader/validator and isolated fixture tests delegated to same Terra/medium agent, scripts/data and tests/run_catalog_tests.gd; parent owns catalog files and formula cross-check.
- Stage 2 review corrected validator scope/type errors detected by output-aware runner (Godot returned 0 despite script errors). Recheck passed; accepted.
- Stage 3 geometry/round-trip tests delegated to Terra/medium; parent owns application model and UI integration.
- Stage 3 geometry result was extended during the Android correction: the board recalculates one shared render/hit-test geometry from its actual container width without a project-wide secondary canvas scaling transform.
- Stage 4 CAGED and position work are independently delegated to two Terra/medium agents with separate data/model/test ownership; the parent retains schemas, UI integration and final musical review.
- Stage 4 integration review found and fixed duplicated overlap positions and scale-quality chord roles: minor shapes use b3 and Locrian uses b5 while retaining the declarative open-major reference geometry.
- Stage 5 API/runtime comparison delegated to Sol/high because Godot audio concurrency, release behavior and Android lifecycle are explicitly high-risk areas. The parent will select and integrate the final engine.
- Chose 12 `AudioStreamPlayer` nodes with cached `AudioStreamWAV` anchors. Confirmed reason for deviating from the initial Polyphonic preference: on Godot 4.7.2, `stop_stream()` did not invalidate/reclaim stream IDs under Dummy or WASAPI. Continuous GDScript generators were rejected due documented mobile performance risk. Eight octave anchors cap pitch shift at six semitones across MIDI 24..108 and avoid per-note first-touch synthesis.
- Delegated backend-independent voice allocation to Terra/medium; reviewed and accepted after integrated lifecycle tests. Stage 6 coordinator is delegated separately to Terra/medium; parent owns UI/audio binding and application notifications.
- Stage 6 coordinator result reviewed and accepted; the main composition root binds pointer signals and clears both view contacts and audio ownership on application focus loss/pause.
- Stage 7 validated settings delegated to Terra/medium. Mechanical RU/EN catalog and parity checks delegated to Luna/low; parent owns responsive settings UI and integration.
- Delegated read-only formula extraction to gpt-5.6-luna / low; compared its 12 formulas and aliases against section 8.2, accepted for catalog preparation.
- Editor auto-saved equivalent project settings and registered the existing runtime bridge autoload by UID. No new addon installed.
- Stage 7 settings implementation and overwrite regression test were delegated to Terra/medium and accepted after integrated checks. Mechanical RU/EN catalog work used Luna/low and passed parity tests.
- Android export readiness was delegated as a Luna/low read-only audit. The audit identified missing matching templates and preset; the parent installed the official version-matched templates, added the project preset/icon, built the APK and performed emulator verification.
- Candidate README and third-party notices were delegated to Luna/low with documentation-only ownership, then reviewed and adjusted during integration.
- The runtime automation autoload remains in the project for the shared MCP workflow, but its entry point registers only while `EngineDebugger.is_active()`. It is inert in a normal exported run.
- Stage 8 chord/arpeggio definitions and practice/statistics models were delegated as two non-overlapping Terra/medium workstreams; the parent reviewed their APIs, integrated playback/UI and owns the final regression. RU/EN strings and two mechanical tuning records used Luna/low.
- Historical pre-rebrand debug artifact: 59,432,327 bytes, SHA-256 `3CEE91687AB331F6574771632470ABAB351EFF4E7EA2C14CC2E7642CFBC6736F`; version 0.1.0 (code 1), min/target SDK 24/36, arm64-v8a and x86_64.

## Verification gaps
- Visible desktop rendering is checked at 1035x381, 1280x480, 1280x720, 1920x720 and 1920x1080 after two reference-comparison iterations. The rebuilt Android APK was installed on `emulator-5554`; the scaled 2400x1080 main screen and Settings overlay were captured after `OnGodotMainLoopStarted`, with no ANR/script/fatal error. The emulator emitted only its known non-fatal shader-cache recompilation warning.
- No physical Android device is connected. Audible output, end-to-end latency, real multitouch and physical-device lifecycle remain unverified; emulator and synthetic input evidence do not satisfy mobile Done.
- The emulator shell `input tap` is not reliable for this rotated AVD's menu release path. The final embedded-menu selection was verified with explicit Android touchscreen `DOWN`/`UP`; a physical touchscreen selection remains part of device acceptance.
- iOS toolchain, signing and device behavior were not tested in the available Windows environment.
